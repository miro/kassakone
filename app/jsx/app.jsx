/** @jsx React.DOM */
define([
	'backbone',
    'router',
    'text!mockDB',
    'collections/Events'
], function(
	Backbone,
	Router,
	mockDB,
	Events
) {
    var router = new Router();

    console.log(mockDB);
    debugger;
});
