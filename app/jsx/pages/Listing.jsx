/** @jsx React.DOM */

define([
    'react'
], function(React) {
    var Listing = React.createClass({
        render: function() {
            return (
                <p>
                    Listing
                </p>
            );
        }
    });
    return Listing;
});
